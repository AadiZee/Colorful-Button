import "./button.css";

const Button = () => {
  return (
    <div className="button-border">
      <button type="button" className="button">
        Start Coding!
      </button>
    </div>
  );
};

export default Button;
